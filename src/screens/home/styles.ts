import { styled } from 'styled-components';

export const Header = styled.header`
  display: flex;
  padding: 1.5rem;
  justify-content: space-between;
  align-items: center;
  width: 100%;

  > div {
    display: flex;
    gap: 0.5rem;
  }
`;
